{
  "name": "the-last-gift-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:web": "npm --prefix web run dev",
    "build:web": "npm --prefix web run build"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
